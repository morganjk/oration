initSidebarItems({"fn":[["get_domain","Parses a URL, returning just the domain portion. The regex is overkill for this at the moment, but I think it may be usefull in the future to have this ability."],["send_notification","Sends an email to a recipient listed in the configuration file when a new comment is posted, so long as the notification system is enabled (this check is elsewhere)."]]});